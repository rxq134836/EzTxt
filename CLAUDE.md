# CLAUDE.md — EzTxt 行为准则（Karpathy Guidelines）

> 来源：https://github.com/multica-ai/andrej-karpathy-skills （`CLAUDE.md` / `skills/karpathy-guidelines/SKILL.md`，MIT）
> 用途：**每次修改代码前先读本节**。减少 LLM 写代码的常见错误（过度设计、乱重构、假设不清、无验证）。
> 取舍：本准则偏"谨慎重于速度"。琐碎任务可用常识判断。

## 1. 想清楚再写代码（Think Before Coding）

**不要假设。不要藏着困惑。把权衡摆到桌面上。**

动手实现前：
- 明确说出你的假设；不确定就问。
- 若存在多种理解，把它们都列出来——不要默默选一种。
- 若有更简单的做法，直说；该反驳时反驳。
- 若某处不清楚，停下来，指出哪里让你困惑，提问。

## 2. 简单优先（Simplicity First）

**能解决问题的最少代码。不做投机性扩展。**

- 不做需求之外的功能。
- 不给一次性代码造抽象。
- 不做没被要求的"灵活/可配置"。
- 不为不可能出现的场景写错误处理。
- 写了 200 行而 50 行就够，就重写。

自问："资深工程师会觉得这过度复杂吗？" 会 → 简化。

## 3. 外科手术式改动（Surgical Changes）

**只碰必须碰的。只清理自己造成的混乱。**

改既有代码时：
- 不要"顺手改进"相邻的代码、注释或格式。
- 不要重构没坏的东西。
- 沿用现有风格，即使你会写得不一样。
- 若发现无关的死代码，提出来——但不要删。

当你的改动制造孤儿：
- 移除**你造成的**不再使用的 import/变量/函数。
- 不要删先前就存在的死代码，除非被要求。

检验标准：**每一行改动都应能直接追溯到用户的请求。**

## 4. 目标驱动执行（Goal-Driven Execution）

**先定义成功标准，再循环直到验证通过。**

把任务变成可验证目标：
- "加校验" → "为非法输入写测试，再让它们通过"
- "修 bug" → "写一个能复现它的测试，再让它通过"
- "重构 X" → "保证前后测试都通过"

多步骤任务先给简要计划：
```
1. [步骤] → 验证：[检查项]
2. [步骤] → 验证：[检查项]
3. [步骤] → 验证：[检查项]
```

强成功标准能让你独立循环；弱标准（"让它能用"）只会导致反复确认。

---

**这些准则有效的标志：** diff 里没有多余改动、不会因过度设计反复重写、澄清问题发生在实现之前而不是犯错之后。
